{
  "name": "@aegis/sandbox",
  "version": "0.0.0",
  "private": true,
  "description": "Microvm sandbox wrapper around @earendil-works/gondolin — gracefully degrades when gondolin/QEMU is unavailable.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "test:coverage": "vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.2",
    "typescript": "^5.9.0",
    "vitest": "^3.1.2"
  }
}
