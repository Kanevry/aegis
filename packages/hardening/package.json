{
  "name": "@aegis/hardening",
  "version": "0.0.0",
  "private": true,
  "description": "5-layer defense middleware for LLM agents — paths, PII, grounding refs, injection, secret redaction.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "test:coverage": "vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.2",
    "typescript": "^5.9.0",
    "vitest": "^3.1.2"
  }
}
